stages:
  - test
  - build
  - deploy

test-job:
  stage: test
  image: maven:3.9.9-amazoncorretto-21
  script:
    - mvn test

build-job:
  stage: build
  script:
    - docker build --network=host -t my-backend .

deploy-job:
  stage: deploy
  script:
    - docker compose up -d
